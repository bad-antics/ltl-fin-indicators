// examples/demo.ltl — runnable demo for ltl_fin_indicators

import std::io
use ltl_fin_indicators as lib

fn main() {
    io::println("ltl_fin_indicators demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}