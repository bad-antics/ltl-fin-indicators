// ltl-fin-indicators/main.ltl
//
// SMA / EMA / RSI / MACD / Bollinger-band indicators.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-fin-indicators v0.1.0")
    io::println("SMA / EMA / RSI / MACD / Bollinger-band indicators.")
    0
}
